import type { EntityTarget } from "typeorm/common/EntityTarget.js";
import type { ForeignKeyOptions } from "typeorm/decorator/options/ForeignKeyOptions.js";
export interface EntitySchemaForeignKeyOptions extends ForeignKeyOptions {
    /**
     * Indicates with which entity this relation is made.
     */
    target: EntityTarget<any>;
    /**
     * Column names which included by this foreign key.
     */
    columnNames: string[];
    /**
     * Column names which included by this foreign key.
     */
    referencedColumnNames: string[];
}
